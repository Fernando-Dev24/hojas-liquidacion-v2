import { UpdateNavbar, UpdateForm } from '../components/'

export const UpdatePage = () => {
  return (
    <section className="container">
      <UpdateNavbar />
      <UpdateForm />
    </section>
  )
}
